name: action-test-no-identity

on:
  workflow_dispatch:

jobs:
  build:
    if: github.repository == 'not-yfiua/test-github-actions'
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: run-success
      run: ./success.py
